services:
  - type: web
    name: bizchat-app
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && (npx drizzle-kit push --force || echo "Database migration failed, continuing with in-memory storage") && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false